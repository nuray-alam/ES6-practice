const firstName = 'Justin';
const lastName = 'TimberLake';

const fullName = `${firstName} ${lastName} is a good boy`;
console.log(fullName);
const multiline = `Are you there?
Where are you?`;
console.log(multiline);